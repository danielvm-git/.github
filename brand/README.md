Design tokens live in [`brand_identity_danielvm`](https://github.com/danielvm-git/brand_identity_danielvm) — `brand/tokens.css`. This folder is a pointer, not a copy; do not duplicate the CSS here.

Note (2026-07 review): that repo also contains `presets/brand-sdd/` — Spec-Kit workflow commands unrelated to branding. That content should move out (to `bigpowers` or here) so `brand_identity_danielvm` stays scoped to brand/design only. See `docs/explanation/repo-disposition-log.md`.
